<div class="form-widget">
    <div class="container">
        <div class="row">
            {% for image in field.value %}
                <div class="col-md-3 p-1">
                    <img style="max-width: 100%; max-height: 200px;" class="img-responsive" src="/images/services/{{image.image}}" alt="">
                </div>
            {% endfor %}
        </div>
    </div>
</div>
